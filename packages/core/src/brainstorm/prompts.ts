/**
 * Brainstorm prompt templates. Stubbed in Section 2; populated in Sections
 * 4 (divergent generation), 6 (cluster), and 7 (rank).
 *
 * Kept in a single file so the prompt surface is reviewable in one diff
 * when tuning. Each exported function takes a typed input and returns a
 * fully-formatted prompt string.
 *
 * Spec: `openspec/changes/add-brainstorm-mode/specs/brainstorm-mode/spec.md`.
 */

export {};
