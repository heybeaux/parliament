__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env
.env.*
!.env.example
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
*.js.map
*.db
*.sqlite

# TypeScript incremental-build cache. Regenerated on every `tsc -b` run, so
# tracking causes recurring merge conflicts and noisy diffs without any
# upside — it's a local cache, not source.
*.tsbuildinfo
.history
.DS_Store
# Tauri / Rust build artifacts
src-tauri/target/
src-tauri/gen/

# Agent workspace contamination (do not commit)
.openclaw/
agentdb.rvf
agentdb.rvf.lock
SOUL.md
IDENTITY.md
USER.md
HEARTBEAT.md
TOOLS.md

# Tauri sidecar bundles (generated at build time by scripts/bundle-sidecar.sh)
src-tauri/binaries/
src-tauri/resources/server/
