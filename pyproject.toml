[project]
name = "parliament"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "sentence-transformers",
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
